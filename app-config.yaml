# app-config.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  database_url: "postgresql://db.example.com:5432/myapp"
  log_level: "info"
  feature_flags: |
    feature_a=true
    feature_b=false
    feature_c=true